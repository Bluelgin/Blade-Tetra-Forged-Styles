# 炼狱刀「死念」——命魂、魂碎与魂崩

## 目标

死念不再只把「命蚀」解释为最大生命 Debuff，而是把它定义成一条独立于肉体 HP 的**命魂完整度**轴。

- 肉体 HP 仍由 Minecraft / 目标 Mod 自己管理。
- 命蚀记录的是命魂被斩去的比例。
- 命蚀累积仍然**没有上限**；100% 只是“命魂归零”的事件阈值，不是存储上限。
- 普通数值伤害负责当前肉体，命魂负责目标还能承载多少生命。

等价关系：

```text
Soul Integrity = 1 - Erosion
```

因此：

```text
Erosion  25% -> Soul Integrity  75%
Erosion 100% -> Soul Integrity   0%  -> 尝试魂崩
Erosion 135% -> Soul Integrity -35%  -> 数值继续保留
```

## 命蚀阶段

命魂仍完整时沿用原来的虚拟生命上限：

```text
Effective HP Cap = Real Max HP * max(0, Soul Integrity)
```

不修改 `Attributes.MAX_HEALTH`，因此第三方 Boss 自己升降最大生命、转阶段或重算属性时仍保有控制权。

普通直接刀击：每次增加 0.25% 命蚀，并保留 4 tick 内部冷却。

「血樱终景·无生」：同一 SA 对同一目标只结算一次 5% 命蚀。

## 第一次命魂归零：死亡握手

当目标第一次达到：

```text
Erosion >= 100%
```

死念不调用 `kill()`，也不每 tick 强制死亡。

服务器只发起一次高强度的正常玩家伤害请求：

```text
Soul reaches zero
        |
        v
normal lethal hurt request
        |
   +----+----+
   |         |
 dies      survives
   |         |
魂崩      魂碎
```

这样目标自己的：

- hurt / damage cap
- 图腾或死亡保护
- 阶段转换
- Forge / 第三方死亡事件
- 战利品、击杀归属与 Boss 清理流程

都仍有一次正常接管机会。

普通实体通常会直接死亡并进入「魂崩」演出。

## 魂碎（SOUL_BROKEN）

如果命魂已经归零，但目标通过伤害上限、转阶段、死亡保护或其他规则仍然活着，则进入「魂碎」。

魂碎不是新的命蚀上限。命蚀仍可继续：

```text
100% -> 110% -> 180% -> ...
```

但从进入魂碎开始：

1. **暂停虚拟 HP 上限钳制。**
   - 不能继续每 tick 把 Boss 压回 0.01 HP，否则第三方阶段逻辑会被破坏。
   - Boss 若把自己切到二阶段并恢复生命，应允许其肉体状态机正常运行。

2. **不自动重复死亡请求。**
   - 普通攻击不会每 tick / 每 hit 调用死亡流程。
   - 避免对 `LivingDeathEvent`、Boss phase hook 或任务脚本形成事件风暴。

3. **目标保持破碎命魂视觉。**
   - 使用 PR #12 已存在的 `life_remnant` 分块资源。
   - 不修改第三方实体模型或骨骼。

## 终末魂崩

魂碎状态下，只有下一次完整命中：

**血樱终景「无生」**

才会请求最终魂崩。

这一步不再依赖普通伤害数值，而是直接进入目标的正常死亡流程：

```text
SOUL_BROKEN
    +
Final Scene hit
    |
    v
terminal death request
```

仍然保留两条最后的世界规则：

- 显式 invulnerable 状态不会被死念启动命魂逻辑；
- `blade_tetra:dead_thought_immune` 仍是整合包 / 剧情实体的硬退出标签。

如果第三方最终仍取消死亡，目标继续保持魂碎；系统不会自动循环重试，只能等待未来另一记完整「无生」。

## 为什么魂碎后不再限制回血

这是兼容设计，不是削弱死念。

当第一次魂崩请求失败时，我们已经知道：

> 有一个比普通数值生命更高层的规则正在维持这个实体。

继续把目标健康值按 0 命魂钳到 0.01，会直接绕开它刚刚成功执行的阶段保护。

因此魂碎状态被定义为：

> 命已经被斩断，但世界规则暂时维持着肉体。

真正终结它的是下一次「无生」，而不是后台 tick。

## VFX 事件

PR #12 原有：

```text
START
LEFT
RIGHT
FINAL
EROSION
STATE
END
```

新增：

```text
SOUL_BROKEN
SOUL_COLLAPSE
```

### SOUL_BROKEN

- 目标仍活着。
- scar 强制最高可视等级。
- 复用 `life_remnant`，在目标身后维持低透明度、已经分裂的命魂轮廓。
- 不生成游戏实体、不修改碰撞。

### SOUL_COLLAPSE

- 服务端只发送一次稀疏事件。
- 客户端收到后立即快照：位置、尺寸、朝向、目标缩放。
- 从此演出不再查询目标实体。
- 即使 Boss 当场死亡、进入死亡动画或被移除，魂崩仍可完整播放。

演出流程：

```text
命魂显现
 -> 六块魂片向外爆裂
 -> 短暂停顿
 -> 血樱终轮展开
 -> 无生切线贯穿
 -> 魂片被终轮拉回吞没
 -> 独立场景自行清理
```

“爆体”只使用客户端几何表现；**不创建 Minecraft Explosion**，不炸方块、不附加范围伤害，也不触发其他 Mod 的爆炸逻辑。

## 清理规则

命魂数据仍属于当前加载实体：

- 实体离开 Level：清理；
- 维度卸载：清理；
- 服务器停止：清理；
- 真正死亡后由实体失活 / 离开世界自然清理。

不再在 `LivingDeathEvent` 一进入时抢先删除命魂状态，因为该事件可能被第三方取消。

## 验收重点

1. 普通怪命魂归零后正常死亡，并播放完整脱离实体的魂崩。
2. 有单次伤害 cap 的 Boss 第一次命魂归零后应存活并进入魂碎，而不是被后台 tick 压到 0.01 HP。
3. 魂碎 Boss 的普通攻击不会重复触发死亡事件。
4. 魂碎 Boss 下一次完整「无生」才请求终末死亡。
5. 如果终末死亡仍被第三方取消，Boss 保持魂碎，不进入循环强杀。
6. 显式无敌阶段不会积累新命蚀，也不会触发魂崩。
7. `dead_thought_immune` 实体完全退出该机制。
8. 目标死亡后，SOUL_COLLAPSE VFX 仍应继续 28 tick 左右并自行结束。
9. 多人只同步稀疏事件；不存在每 tick 网络同步。
10. 低特效模式仍应看见核心樱轮、裂口和魂崩结构。
