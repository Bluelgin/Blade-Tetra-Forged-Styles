# 血樱终景「无生」——美术实现与验收

## 分支关系

本文件最初来自 PR #12 的纯客户端美术实现。后续 `feat/dead-thought-soul-collapse` 在其上叠加了命魂／魂碎／魂崩机制；已有模型、贴图和演出结构继续复用，不推倒重做。

机制细节见：`DEAD_THOUGHT_SOUL_SYSTEM.md`。

## 美术范围

为炼狱刀「死念」提供几何演出，不替换 SJAP 资源，也不要求 SJAP 编译依赖。

- 地面断裂血樱阵分层刻出，不旋转；中／高画质带四／六簇枯枝。
- 原生 Sakura End 左右斩通知触发有厚度的黑红裂口，8 tick 内开合。
- 终段展开断裂樱轮，随后短时静止、细切线与命魂残影抽离。
- 樱轮按碰撞箱缩放，直径限制在 3–7 方块；裂口厚度保持约 0.12–0.151 方块，不随 Boss 无限扩大。
- 普攻命蚀仅显示局部斩痕。15%、40%、70% 分档；超过 100% 也不会增加普通命蚀视觉层数。
- 可关闭的施术者专属降饱和／暗角约 14 tick；低画质关闭。无强制镜头、无全服滤镜。

## 命魂语义

`life_remnant` 不再只是“最大生命减少”的抽象装饰，而正式表示目标的命魂投影。

### 普通命蚀

- `EROSION`：短暂 scar / 裂口反馈。
- `STATE`：重新开始追踪高命蚀目标时恢复可视状态。

### 魂碎

新增 `SOUL_BROKEN`：

- 目标仍活着，但命魂完整度已经归零。
- scar 固定最高可视层级。
- 复用 `life_remnant` 的分组，在目标身后显示低透明度、已经碎裂的命魂。
- 不修改第三方实体模型、UV、骨骼或碰撞。

### 魂崩

新增 `SOUL_COLLAPSE`：

- 客户端收到事件时快照目标位置、尺寸与演出缩放。
- 随后使用独立 `Collapse` 场景，不再跟随或查询死亡目标。
- 目标即使当场死亡／被移除，魂崩仍继续播放约 28 tick。
- 六块 `life_remnant` 先向外爆裂、短暂停顿，再被 `final_wheel` 拉回吞没。
- 同时复用 `rifts` 与 `execution_line` 表现肉体被命魂崩坏牵连撕裂。
- 这只是客户端几何演出，不创建 Minecraft `Explosion`，不炸方块、不制造额外范围伤害。

## 架构与边界

`DeadThoughtVisuals` 只发送稀疏事件，复用 `ModularTechniqueVfxPacket` 和 `TechniqueVfxRegistry`。客户端拆分生命周期、模型绘制、场景、斩痕与屏幕氛围，不扩充旧的大型渲染分支。

同一施术者的新常规 SA 演出替换旧 SA 场景，阶段事件去重；魂崩使用独立快照场景，因此不受目标 `isAlive()` 生命周期限制。

客户端上限：

- 普通 Dead Thought 场景：8
- detached 魂崩场景：8
- scar / 魂碎状态：12

所有这些对象均为客户端记录，不生成游戏实体、碰撞或攻击判定。

## 配置

复用：

- `bladeCombatVfxQuality`（0/1/2）
- VFX 距离
- VFX 强度
- 总开关
- `visuals.enableDeadThoughtGrading`

低画质保留：

- 刻阵
- 左右裂口
- 樱轮
- 切线
- 魂崩核心结构

低画质关闭或简化：

- 枯枝
- 多层普通斩痕
- 完整六片魂体（降为三片）
- 屏幕处理

## 验收清单

1. 普通怪锁定／未锁定／空挥时，左右裂口、樱轮和收束与原生 Sakura End 时序一致。
2. 第一／第三人称、侧面、大型 Boss 下检查 UV、厚度、准星遮挡和樱轮位置。
3. 普攻命蚀只显示短斩痕，不误播完整 SA。
4. 达到魂碎的 Boss 身后应保持碎裂命魂，但其本体仍可正常进入第三方阶段。
5. 发生魂崩后，即使目标实体立刻死亡，魂体爆裂／回收与终轮仍完整播放。
6. 多目标／连续释放不应留下永久樱轮或 detached 场景。
7. 目标传送、离开区块、玩家死亡／换维度、资源重载后无残留。
8. 低画质与关闭 grading 后核心几何仍可读。
9. 多人观察关键模型一致，施术者专属屏幕氛围不广播给全服。
10. 分别测试无光影与常用光影，并记录瞬时帧率。

开发启动：

```text
gradlew runClient -PsjapTest -PcataclysmTest
```

该命令只作为手动测试入口；CI 的 `gradlew build` 只能证明编译、资源和自动测试，不等于完成实机视觉验收。
