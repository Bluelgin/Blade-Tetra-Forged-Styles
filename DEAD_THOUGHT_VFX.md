# 血樱终景「无生」——美术实现与验收

## 本轮范围

为炼狱刀「死念」增加纯客户端演出。原有伤害、命蚀数值、命中判定、融合条件均未修改；未替换 SJAP 资源，未增加 SJAP 编译依赖。

- 地面断裂血樱阵分层刻出，不旋转；中／高画质带四／六簇枯枝。
- 原生 Sakura End 左右斩通知触发有厚度的黑红裂口，8 tick 内开合。
- 终段展开断裂樱轮，随后短时静止、细切线、实际命蚀命中后的生命残影抽离与收束。最长 44 tick 自动清理。
- 樱轮按碰撞箱缩放，直径限制在 3–7 方块；裂口厚度保持约 0.12–0.151 方块，不随 Boss 无限扩大。
- 普攻命蚀仅显示局部斩痕。15%、40%、70% 分档；超过 100% 也不会增加视觉层数。极重度保留淡轮片，重新开始追踪目标时同步状态。
- 可关闭的施术者专属降饱和／暗角，约 14 tick；低画质关闭。无强制镜头、无全服滤镜。

## 架构与边界

`DeadThoughtVisuals` 只发稀疏事件，复用 `ModularTechniqueVfxPacket` 和 `TechniqueVfxRegistry`。客户端拆分生命周期、模型绘制、场景、斩痕与屏幕氛围，不扩充旧的大型渲染分支。

同一施术者的新演出替换旧演出，阶段事件去重；主目标显示完整场景，其他命中目标只显示命蚀反馈。客户端最多保留 8 个场景和 12 个斩痕记录，不生成任何游戏实体／碰撞／额外伤害。严重命蚀记录虽然可长驻，但有容量上限，并随目标卸载、死亡、传送、距离超限、换世界或重载清除。

模型面数是静态预算，不等于实测性能：七组资源共 4212 个三角形，实际场景按阶段选择分组、重复绘制枯枝；暂无实测 FPS 数值。

原生 Sakura End 仍保留自己的刀光、音效和时序。音量抽空、自定义音效、镜头震动、空间扭曲与其他技能的全局视觉降噪暂未加入，避免影响其他模组。当前声音节点可沿 START／LEFT／RIGHT／FINAL 事件扩展。普通目标与大型 Boss 的构图仍需实机验收。

## 配置

复用 `bladeCombatVfxQuality`（0/1/2）、距离、强度和总开关。新增 `visuals.enableDeadThoughtGrading`；关闭后所有世界内几何仍显示。低画质保留刻阵、左右裂口、樱轮和切线，关闭枯枝、残影、多层斩痕与屏幕处理。

## 验证状态

- 完整 `gradlew build` 已通过，包括几何资源与视觉数学回归测试。
- Blockbench 轮廓预览已人工检查；不是游戏截图。
- 按用户最新要求，实机测试由用户执行。本 PR **不合并**；未宣称完成游戏内截图、录像、多人或性能验收。

## 玩家验收清单

1. 使用已测试通过的死念配装与「血樱终景」SA，先对普通怪锁定释放，再试未锁定与空挥。检查左右裂口、樱轮和收束是否跟随原生连段。
2. 第一／第三人称及侧面观察，检查 UV、模型厚度、准星遮挡、地阵高度和目标身后樱轮位置；再对大型 Boss 重复。
3. 普攻命蚀应只有短斩痕，不播放整场 SA；高命蚀目标显示更多局部痕迹，而非全身红色。
4. 多目标／连续释放不应叠出多个永久樱轮。目标死亡、传送、离开区块、玩家死亡或换维度后检查残留。
5. 将画质调为 0，以及关闭 `enableDeadThoughtGrading`，确认核心几何仍可见；资源重载后再释放一次。
6. 多人相互观察关键模型，确认只有施术者收到短时屏幕氛围。分别测试无光影和常用光影，留意瞬时帧率。

开发启动：`gradlew runClient -PsjapTest -PcataclysmTest`。此命令只作为手动测试入口，本轮未自动启动。
